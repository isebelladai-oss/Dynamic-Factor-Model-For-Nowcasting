# 项目操作规则

本文档记录本项目后续操作时必须保持一致的约定。每次修改 notebook、生成中产物或新增数据文件时，先参考本文档。

## 1. Notebook 与中产物编号

- Notebook 中的流程步骤编号、markdown cell 标题编号、中产物文件夹编号必须保持一致。
- 若后续在已有步骤前插入新步骤，必须同步调整后续：
  - notebook markdown 标题编号；
  - 代码中的输出目录变量；
  - `月频模型/模型流程中产物` 下的步骤文件夹编号；
  - notebook 输出提示文案；
  - `项目文件说明.md` 中对应条目。
- 中产物文件夹命名格式：

```text
月频模型/模型流程中产物/N-步骤名称
```

例如：

```text
月频模型/模型流程中产物/1-训练期窗口诊断
```

## 2. 中产物保存规则

- 各步骤生成的 CSV、图片、表格等诊断或复查结果，不直接散落在 `月频模型/processed`。
- 模型最终输入、标准化输入、缓存、估计结果仍可保留在 `月频模型/processed`。
- 诊断、复查、对比分析类输出统一放在 `月频模型/模型流程中产物/N-步骤名称`。
- 每个步骤文件夹中的 CSV 或图片文件名应能直接看出用途，例如 `training_window_candidate_summary.csv`。

## 3. 文件说明文档规则

- 项目根目录必须维护 `项目文件说明.md`。
- 该文件用于简要描述原始数据、processed 文件、notebook、中产物文件夹的特征或作用。
- 每当新增、移动、重命名、改变用途明显的文件或文件夹时，必须同步更新 `项目文件说明.md`。
- 文件说明应简短、事实化，优先说明：
  - 文件路径；
  - 来源或生成步骤；
  - 主要内容；
  - 是否为原始数据、模型输入、模型输出或诊断中产物；
  - 后续是否会被 notebook 读取。

## 4. Notebook 修改规则

- 默认只修改用户指定的工作 notebook。
- 新增分析步骤优先插入到其逻辑依赖完成之后、后续建模步骤之前。
- 新增诊断步骤不应随意改变后续模型默认使用的 `TRAIN_START` / `TRAIN_END`。若用户确认扩窗，只修改训练窗口配置处，并让后续流程自动继承。
- 训练窗口配置必须集中在日期统计之后的同一个 cell：`TRAIN_START`、`TRAIN_END`、`TRAIN_WINDOW_TAG`、标准化输出路径和 EM 缓存前缀都从这里派生。
- 后续每次扩大训练窗口，不重新做变量筛选；沿用当前 `MODEL_COLS`，但必须基于新训练窗口重新计算缩尾阈值、标准化参数和 EM 缓存。
- 修改保存路径时，要同步更新打印提示，方便在 notebook 输出中定位文件夹。
- 若新增诊断只是帮助判断模型问题，不应顺手重跑完整 EM 模型。

## 5. 当前项目背景与复查顺序

- 当前工作 notebook 是 `1 baseline_training_flow.ipynb`。
- 当前 baseline 使用 `baseline_model_input.csv`，默认训练窗口为 `2005-01` 至 `2014-12`；后续调整窗口只改 notebook 中 `TRAIN_START` / `TRAIN_END`。
- 当前 baseline 的核心模型变量共 10 个，包括 GDP、PMI 水平值、工业增加值、用电量、社零、固定资产投资、CPI、PPI、失业率差分。PMI 水平值从 `宏观变量&GDP raw data.csv` 合并进入工作 notebook，替代 `baseline_model_input.csv` 中的 PMI 一阶差分。
- 复查思路优先级：
  1. 先做训练期候选窗口诊断；
  2. 再做单变量描述性统计、波动、季节性检查；
  3. 再做变量间相关性和变量-GDP 领先滞后相关；
  4. 再检查变量处理是否损失信息；
  5. 最后再决定是否调整变量处理或重跑模型。
- 每一步检查都要给出经济学含义判断，而不只是生成表格。

## 6. 当前编号基准

- `1 baseline_training_flow.ipynb` 中当前新增的 `2A. 训练期候选窗口诊断` 输出目录为：

```text
月频模型/模型流程中产物/1-训练期窗口诊断
```

- 产物文件名若涉及训练窗口，使用 `TRAIN_WINDOW_TAG` 派生，不写死 `1996_2015` 这类旧窗口标签。
- 后续新增诊断步骤从 `2-...` 继续编号。
- 如果 notebook 流程结构发生前置插入，则重新同步编号。
